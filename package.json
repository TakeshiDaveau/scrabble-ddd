{
  "scripts": {
    "debug": "tsc -w & nodemon -q -w build build/src/index.js",
    "start": "node build/src/index.js",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "compile:watch": "tsc",
    "fix": "gts fix",
    "prepare": "yarn run compile",
    "pretest": "yarn run compile",
    "test": "jest",
    "posttest": "yarn run lint"
  },
  "devDependencies": {
    "@types/jest": "^29.2.4",
    "@types/node": "^14.11.2",
    "gts": "^3.1.1",
    "jest": "^29.3.1",
    "nodemon": "^2.0.20",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.9.1",
    "tsarch": "^5.2.0",
    "typescript": "~4.7.0"
  }
}
